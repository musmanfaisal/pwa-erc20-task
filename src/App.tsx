import { useState } from "react";
import { Container, Table, Offcanvas } from "react-bootstrap";

import "./App.scss";

function App() {
	return (
		<Container fluid>
			<Table></Table>
		</Container>
	);
}

export default App;
